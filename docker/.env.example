# MSQPay Docker Environment Variables
# Copy this file to .env and fill in your values
# DO NOT commit .env file to version control
#
# NOTE: This .env file is only used by docker-compose-amoy.yml
#       docker-compose.yml (local Hardhat) does not require these variables

# =============================================================================
# Simple Defender (Gasless Relay Service)
# =============================================================================
# Generate a new wallet: cast wallet new
# Or use: openssl rand -hex 32

# Relayer private key (with or without 0x prefix)
RELAYER_PRIVATE_KEY=your_64_char_hex_private_key_here

# Relayer address (derived from private key above)
RELAYER_ADDRESS=0xYourRelayerAddressHere

# =============================================================================
# Gas Faucet (Pay Server – optional)
# =============================================================================
# Wallet that sends native token (POL/ETH) to users who need gas for approve.
# Used by POST /payments/prepare-wallet. If unset, gas grant will fail when requested.
# Format: 0x + 64 hex chars (same as SIGNER_PRIVATE_KEY).

# GAS_FAUCET_PRIVATE_KEY=0xYourFaucetWalletPrivateKeyHere
