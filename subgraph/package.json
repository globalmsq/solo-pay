{
  "name": "@msq/pay-subgraph",
  "version": "0.1.0",
  "private": true,
  "description": "MSQ Pay Onchain Subgraph for payment event indexing",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph codegen && graph build",
    "deploy": "graph deploy --studio msq-pay-polygon-amoy",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 msq-pay",
    "create:local": "graph create --node http://localhost:8020/ msq-pay",
    "remove:local": "graph remove --node http://localhost:8020/ msq-pay",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.79.0",
    "@graphprotocol/graph-ts": "^0.35.0",
    "matchstick-as": "^0.6.0"
  }
}
